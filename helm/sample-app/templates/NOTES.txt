sample-app v{{ .Values.image.tag }} deployed to namespace {{ .Release.Namespace }}.

Check pod status:
  kubectl get pods -n {{ .Release.Namespace }} -l app={{ .Release.Name }}

Port-forward to test locally:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Release.Name }} 8080:{{ .Values.service.port }}
  curl http://localhost:8080/

Image deployed: {{ .Values.image.repository }}:{{ .Values.image.tag }}
